package com.demo.boot3.pojo;

import java.util.concurrent.ThreadFactory;

public class ThreadPoolSetting implements ThreadFactory {
    @Override
    public Thread newThread(Runnable r) {
        return null;
    }
}
